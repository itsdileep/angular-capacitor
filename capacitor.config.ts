import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dileep.capacitorApp',
  appName: 'angular-capacitor',
  webDir: 'dist/angular-capacitor',
  bundledWebRuntime: false
};

export default config;
